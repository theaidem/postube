# App vars
## Name for your application without spaces, example: `postube`, `funy_videos`
APP_NAME=postube

# Remote vars for deploy/update application via ssh
## Remote user
USER=root

## Remote host, e.g `domain.com`, `<ip>:<port>`
HOST=hostname.com

## An absolute path, where the app will be installed
APP_PATH=/root/postube